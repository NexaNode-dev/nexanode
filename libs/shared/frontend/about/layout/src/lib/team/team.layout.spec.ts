import { NexaNodeAboutTeamLayout } from './team.layout';

describe('NexaNodeAboutTeamLayout', () => {
  it('should render the component', async () => {
    const element = new NexaNodeAboutTeamLayout();
    expect(element).toBeTruthy();
  });
});
