import type { Meta, StoryObj } from '@storybook/web-components';

const meta: Meta = {
  component: 'one-layout',
  title: 'OneServicesLayout',
}

export default meta;
type Story = StoryObj;

export const Primary: Story = {
  args: {},
};